import { Head } from "$fresh/runtime.ts";

export default function MyHead() {
  return (
    <Head>
      <meta name="description" content="Новый взгляд на старю вещь" />
      <title>Legist fresh</title>
    </Head>
  );
}